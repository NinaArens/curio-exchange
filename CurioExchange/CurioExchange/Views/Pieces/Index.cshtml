@model List<CurioExchange.Models.PieceModel>

@{
    ViewBag.Title = "Pieces overview";
}

<h2>Pieces overview</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Rare)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Set)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Set.Collection)
        </th>
        @*<th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rare)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Set.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Set.Collection.Name)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
